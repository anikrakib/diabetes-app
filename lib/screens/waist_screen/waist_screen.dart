import 'package:flutter/material.dart';

class WaistScreen extends StatelessWidget {
  const WaistScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Waist Screen'),
    );
  }
}
