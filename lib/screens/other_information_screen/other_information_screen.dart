import 'package:flutter/material.dart';

class OtherInformationScreen extends StatelessWidget {
  const OtherInformationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Other Info Screen'),
    );
  }
}
